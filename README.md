# MiniAsana

This project was generated using [Angular CLI](https://github.com/angular/angular-cli) version 19.1.7.

## Building

To build the project run:

docker build -t mini-asana .

## Running the app

docker container run -p 8080:80 mini-asana

Navigate to http://localhost:8080/ for preview.
